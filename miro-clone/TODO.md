# TODO for Miro Clone

## Completed Features
- Core functionalities (infinite canvas, multiple shapes, grouping, images, layers, responsive UI).
- Real-time collaborative multiplayer (cursors, selection locking, chat).
- UI and Tooling (properties panel, stroke width, undo/redo stack, document upload preview, minimap).
- Additional capabilities (copy/paste via shortcuts, grid snapping, eraser tool).

## To-Do
- [ ] Add ability to export the board canvas to a PNG or SVG image.
- [ ] Add Sticky Notes support (shapes with embedded text that automatically size correctly).
